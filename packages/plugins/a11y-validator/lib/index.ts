// Serve entry (DL#179): must stay compiler-free. This plugin is validator-only — all of its surface
// is tools-time and lives in `./tools` (lib/tools.ts). The serve entry is intentionally empty.
export {};
