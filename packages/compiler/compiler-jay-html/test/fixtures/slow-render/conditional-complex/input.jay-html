<!DOCTYPE html>
<html>
<head>
    <title>Complex Conditionals</title>
</head>
<body>
    <!-- Test 1: Slow AND that resolves to true (both true) -->
    <div if="inStock && hasDiscount" class="available-deal">Great deal!</div>

    <!-- Test 2: Slow AND that resolves to false (first is false) -->
    <div if="isPromoted && hasDiscount" class="promoted-deal">Super deal!</div>

    <!-- Test 3: Slow OR that resolves to true (one is true) -->
    <span if="isPromoted || hasDiscount" class="badge">Has offer</span>

    <!-- Test 4: Slow comparison that resolves to true -->
    <span if="count > 0" class="in-stock">In stock</span>

    <!-- Test 5: Slow comparison that resolves to false -->
    <span if="count <= 0" class="out-of-stock">Out of stock</span>

    <!-- Test 6: Mixed phase - slow is true, simplifies to fast check only -->
    <div if="inStock && price > 0" class="purchasable">Buy now</div>

    <!-- Test 7: Mixed phase - slow is false, short circuits to false -->
    <div if="isPromoted && price > 100" class="premium-promoted">Premium</div>

    <!-- Test 8: Mixed phase OR - slow is true, short circuits to true -->
    <div if="hasDiscount || price < 50" class="good-value">Good value</div>

    <!-- Test 9: Complex nested - (slow && slow) || slow -->
    <div if="(inStock && hasDiscount) || isPromoted" class="buyable">Buyable</div>
</body>
</html>
